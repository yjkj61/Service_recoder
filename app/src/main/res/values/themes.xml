<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.Service_recoder" parent="Theme.MaterialComponents.DayNight.NoActionBar.Bridge">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.Service_recoder" parent="Base.Theme.Service_recoder" />

    <style name="ScreenDialogStyle" >
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowFrame">@null</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowIsFloating">true</item>
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowAnimationStyle">@android:style/Animation.Dialog</item>
        <item name="android:backgroundDimEnabled">true</item>
    </style>

    <style name="SecondControl" parent="Theme.AppCompat.Light">
        <item name="colorControlNormal">@color/color_D9D9D9</item>
        <item name="colorControlActivated">@color/color_4580FF</item>
    </style>

    <style name="bottom_tab_title_active">
        <item name="android:textSize">13sp</item>
    </style>

    <!--没有选中的样式，导航默认的字体大小就是14sp-->
    <style name="bottom_tab_title_inactive">
        <item name="android:textSize">12sp</item>
    </style>

    <!--ShapeableImageView 圆角-->
    <style name="RoundedStyle">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">12dp</item>
    </style>

    <style name="Dialog_style" parent="android:Theme.Dialog">
        <item name="android:windowFrame">@null</item>
        <!-- 边框 -->
        <item name="android:windowIsFloating">true</item>
        <!-- 是否浮现在activity之上 -->
        <item name="android:windowIsTranslucent">true</item>
        <!-- 半透明 -->
        <item name="android:windowNoTitle">true</item>
        <!-- 无标题 -->
        <item name="android:windowBackground">@android:color/transparent</item>
        <!-- 背景透明 -->
        <item name="android:backgroundDimEnabled">false</item>

    </style>

    <style name="roomRatingBar" parent="Widget.AppCompat.RatingBar.Small">

        <item name="android:minHeight">12dip</item>
        <item name="android:minWidth">12dip</item>
    </style>

    <!--  下载视频进度条的样式  -->
    <style name="ProgressBar_task" parent="@android:style/Widget.ProgressBar.Horizontal">
        <item name="android:maxHeight">@dimen/dp_10</item>
        <item name="android:minHeight">@dimen/dp_10</item>
        <item name="android:indeterminateOnly">false</item>
    </style>

</resources>