<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:bind="http://schemas.android.com/apk/res-auto">

    <data>
        <variable
            name="viewModel"
            type="com.yjkj.service_recoder.ui.ph.phchecklist.PhCheckListViewModel" />
        <variable
            name="click"
            type="com.yjkj.service_recoder.ui.ph.phchecklist.PhCheckListFragment.Click" />
    </data>

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:paddingStart="10dp"
        android:paddingEnd="10dp"
        android:paddingTop="30dp"
        android:paddingBottom="30dp"
        tools:context=".ui.ph.phchecklist.PhCheckListFragment">


        <include
            layout="@layout/ph_list_top_option_layout"
            bind:viewModel="@{viewModel}"
            bind:click="@{click}"/>

        <androidx.recyclerview.widget.RecyclerView
            android:layout_width="match_parent"
            android:layout_height="0dp"
            android:layout_marginTop="24dp"
            app:layout_constraintTop_toBottomOf="@+id/ph_list_top_option_layout"
            app:layout_constraintBottom_toBottomOf="parent"
            dividerItemDecoration="@{viewModel.itemDecoration}"
            gridLayoutManager="@{viewModel.gridLayoutManager}"
            items="@{viewModel.items}"
            itemBinding="@{viewModel.itemBinding}"/>


    </androidx.constraintlayout.widget.ConstraintLayout>
</layout>